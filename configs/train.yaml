# @package _global_

defaults:
  - _self_
  - data: protein_displacement
  - model: egnn_module
  - trainer: default
  - logger: csv

paths:
  data_dir: ${hydra:runtime.cwd}/data
  output_dir: ${hydra:runtime.cwd}/outputs

seed: 42
